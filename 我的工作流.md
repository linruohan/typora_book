
## 1 任务来源
### 1.1 hive平台：
1. 测试环境类型管理
2. 测试机器标签tag管理
3. 测试机器host管理
4. 测试任务执行管理：拉任务、查看任务列表、查看任务执行详情
5. 测试执行历史查询：用例名查询历史执行记录
### 1.2 DTS平台
1. 提单
2. 回归问题单
3. 查看各视图中问题单列表
### 1.3 codehub平台
1. 查看提交的mr：检视意见
2. 查看所有仓库链接
### 1.4 itrace平台
1. 需求管理
2. 需求测试
3. 需求功能规格和约束查询
### 1.5 项目管理
1. 开工会
2. AAR回溯
3. 结项
### 1.6 考勤管理
1. 我的考勤
2. 待审批的请假电子流

明白了，你的任务管理需求比之前描述的更具体——**本质上是一个“多平台数据聚合与操作面板”**。核心痛点是：你每天需要在 Hive、DTS、CodeHub、iTrace 等五六个平台之间来回切换，才能汇总出“今天要做什么”。

基于这个现状，我建议的搭建策略是：**让 Claude 成为你的“统一查询入口”，把高频查询变成自然语言交互，把高频操作固化为快捷指令**。

---

## 2 🎯 整体思路：从“查”到“做”的分层建设

---

## 3 📋 第一步：建立“平台映射表”（Project 知识库基础）

在 Claude Project 中，你需要上传一份**平台操作手册**（Markdown 或 Excel），告诉 Claude 每个平台能查什么、怎么查。内容大概长这样：

|平台|可查询/操作的内容|访问方式|常见查询关键词|
|---|---|---|---|
|**Hive**|测试环境类型、机器标签、Host管理、任务执行（拉任务/查看列表/查看详情）、历史执行查询|内部Web + API|AT失败、用例名、任务ID|
|**DTS**|提单、回归问题单、各视图问题单列表|内部Web|问题单ID、视图名称|
|**CodeHub**|MR列表、检视意见、仓库链接|Git平台|MR ID、仓库名|
|**iTrace**|需求管理、需求测试、功能规格与约束|内部Web|需求ID、迭代版本|
|**项目管理**|开工会、AAR回溯、结项|文档/会议系统|项目名、阶段|
|**考勤管理**|我的考勤、待审批请假|OA系统|日期、审批状态|

> **关键动作**：把每个平台的**典型查询语句/URL模板/API接口**整理好，作为 Claude 的“工具箱说明书”。

---

## 4 🧠 第二步：设计“任务分类 → 平台映射”的指令

在 Project 的“项目指令”中，直接定义 Claude 的任务路由规则：

markdown

你是一个多平台任务管理助手。当用户提出需求时，按以下规则响应：
### 4.1 日常AT相关 → 查询Hive平台
- 「今天哪些AT失败」→ 调用Hive任务列表，筛选状态=失败，时间=今天
- 「用例 xxx 的历史执行记录」→ 调用Hive历史查询，按用例名检索
- 「拉一个测试任务」→ 引导用户提供环境/标签/host信息，生成拉任务指令
### 4.2 问题单处理 → 查询DTS平台
- 「今天待回归的问题单」→ 调用DTS问题单列表，筛选状态=待回归
- 「提单」→ 引导用户提供缺陷描述，生成提单信息模板
- 「视图 xxx 的问题单」→ 按视图名称筛选
### 4.3 MR与代码审查 → 查询CodeHub
- 「我的待处理MR」→ 查询当前用户的MR列表及检视意见
- 「仓库 xxx 的MR」→ 按仓库名查询
- 「查看所有仓库链接」→ 返回配置的仓库列表
### 4.4 需求与测试 → 查询iTrace
- 「当前迭代的需求列表」→ 按迭代筛选需求
- 「需求 xxx 的功能规格」→ 返回该需求的规格和约束
- 「需求测试范围」→ 关联需求下的测试用例覆盖情况
### 4.5 项目管理阶段
- 「项目的开工会材料」→ 返回项目启动文档模板
- 「AAR回溯」→ 引导填写回溯模板
- 「结项清单」→ 返回结项检查项
### 4.6 考勤
- 「我的考勤」→ 返回本月考勤汇总
- 「待审批请假」→ 列出待审批电子流

---

## 5 🔌 第三步：搭建“数据连接”（核心难点）

要让 Claude 真正能“查询”而非“让你去查”，需要打通数据通道。有三种方案，按投入成本从低到高：

|方案|做法|适用场景|投入|
|---|---|---|---|
|**方案A：浏览器自动化**|用 MCP 的 Puppeteer 服务器，让 Claude 控制浏览器登录各平台截图/抓取数据|不想写API，各平台无开放接口|中等|
|**方案B：API封装**|为每个平台封装一个 MCP Server，暴露查询/操作接口给 Claude|各平台有API或内部接口|较高|
|**方案C：定期数据同步**|写定时脚本把各平台数据同步到一个本地SQLite/Excel，Claude读取该文件|查询为主，不需要实时写操作|最低|

> **推荐起步用方案C + A的组合**：
> 
> - 用定时脚本（Python/Shell）每天早晚各拉一次 Hive/DTS 的关键数据，存成 JSON/CSV 文件，放在 Claude 项目目录下。
>     
> - Claude 通过读取这些本地文件来回答问题，实时操作（如拉任务、提单）则通过 MCP Puppeteer 控制浏览器完成。
>     

---

## 6 ⚡ 第四步：把高频操作固化為 Skills

以下几个 Skill 对你来说性价比最高：

### 6.1 Skill 1：`每日任务简报`

- **触发**：每天上班时输入 `/daily`
    
- **执行**：
    
    1. 读取 Hive → 今日失败任务数 + TOP3失败用例
        
    2. 读取 DTS → 待回归问题单数
        
    3. 读取 CodeHub → 待处理MR数
        
    4. 读取 iTrace → 当前迭代需求进度
        
    5. 汇总成一张 Markdown 表格，按优先级排序
        

### 6.2 Skill 2：`AT失败根因分析`

- **触发**：`分析用例 xxx 的失败原因`
    
- **执行**：
    
    1. 从 Hive 查该用例的历史执行记录
        
    2. 按错误类型聚类（超时/断言失败/环境问题）
        
    3. 关联 DTS 看是否有对应问题单
        
    4. 给出修复建议或建议提单
        

### 6.3 Skill 3：`版本发布检查清单`

- **触发**：`准备发版`
    
- **执行**：
    
    1. 查 iTrace → 需求是否全部测试完成
        
    2. 查 DTS → 是否有未回归的严重问题单
        
    3. 查 CodeHub → MR是否全部合入
        
    4. 输出“阻塞项”和“待确认项”清单
        

---

## 7 📁 推荐的目录结构

text

.claude/
├── CLAUDE.md                    # 全局规则：平台映射 + 任务路由
├── skills/
│   ├── daily-brief/
│   │   └── SKILL.md             # 每日简报技能
│   ├── at-failure-analysis/
│   │   └── SKILL.md             # AT失败分析技能
│   └── release-checklist/
│       └── SKILL.md             # 发版检查清单技能
├── data/                        # 存放同步下来的数据文件
│   ├── hive_tasks_latest.json
│   ├── dts_issues_latest.json
│   └── itrace_requirements.json
└── mcp-servers/                 # 如需API/MCP封装
    ├── hive-server/
    └── dts-server/

---

## 8 🚀 建议的起步路径

1. **本周**：先建一个 Claude Project，上传“平台映射表”和几条历史任务记录，测试能否通过对话覆盖 80% 的查询需求。
    
2. **下周**：写一个最简单的定时同步脚本（Python），每天拉 Hive 任务列表 + DTS 问题单列表，存为 JSON 文件，让 Claude 读取。
    
3. **第三周**：封装 2 个高频 Skill（`daily-brief` 和 `at-failure-analysis`），结束每天重复输入的痛苦。
    
4. **后续**：根据痛点逐步补充，比如接入 MCP Puppeteer 实现“自动提单”或“自动拉任务”。
    

---

你的平台们有提供 API 吗？还是只能通过网页操作？这个答案会决定**数据同步**和**自动化操作**的实现路径，决定了具体用方案A/B/C中的哪种组合。