# Python CustomTkinter 任务管理系统设计方案

## 1 项目概述

本系统是一个基于 Python 和 CustomTkinter 的桌面任务管理工具，旨在为个人或小团队提供简洁高效的任务协作功能。系统以本地数据库为核心，支持任务的完整生命周期管理、责任人分配、进度追踪、定时提醒以及附件与链接的集中管理。

## 2 功能需求

- **任务 CRUD**：创建、查看、编辑、删除任务，支持标题、描述、优先级、截止时间等字段。
    
- **分配 Owner**：可从预设人员列表中选择任务负责人，支持 Owner 的增删改查。
    
- **进展管理**：提供任务状态（如未开始/进行中/已完成）或百分比进度，支持日志式更新。
    
- **任务提醒**：根据截止时间和用户设置的提前量，通过桌面通知和弹窗提醒。
    
- **附件管理**：允许为任务添加本地文件作为附件，支持打开、删除、导出附件。
    
- **链接管理**：允许为任务添加多个 URL 链接，支持一键在浏览器中打开。
    
- **高级筛选与搜索**：按状态、Owner、关键词、截止日期范围过滤任务。
    

## 3 技术栈

- **语言**：Python 3.9+
    
- **GUI 框架**：CustomTkinter（界面现代化、支持主题）
    
- **数据库**：SQLite3（本地轻量、无需安装）
    
- **提醒通知**：`plyer` 库（跨平台桌面通知）+ `tkinter` 定时检查
    
- **附件处理**：`shutil` 复制文件、`os` 管理路径，`subprocess` 调用系统默认程序打开
    
- **链接处理**：`webbrowser` 模块打开 URL
    
- **日期处理**：`datetime` 模块
    
- **打包发布**（可选）：PyInstaller
    

## 4 系统架构

采用分层架构，视图层与逻辑层分离：

text

┌──────────────────────────────────┐
│         GUI 视图层               │
│  (主窗口、任务列表、编辑对话框、  │
│   筛选栏、提醒弹窗、Owner管理)    │
└───────────────┬──────────────────┘
                │ 调用
┌───────────────▼──────────────────┐
│         业务逻辑层               │
│  TaskManager, ReminderService,   │
│  AttachmentHandler, OwnerManager │
└───────────────┬──────────────────┘
                │ 数据访问
┌───────────────▼──────────────────┐
│         数据持久层               │
│  SQLiteHelper (连接、建表、CRUD) │
└──────────────────────────────────┘

- **主入口**：`main.py` 初始化数据库、启动主窗口、启动提醒守护线程。
    
- 各模块独立为 `.py` 文件，便于维护。
    

## 5 数据库设计

使用 SQLite，数据库文件 `task_manager.db`，主要表结构如下：

### 5.1 任务表 (tasks)

|字段名|类型|说明|
|---|---|---|
|id|INTEGER|主键，自增|
|title|TEXT|任务标题，非空|
|description|TEXT|任务描述|
|status|TEXT|未开始/进行中/已完成，默认“未开始”|
|priority|TEXT|低/中/高/紧急，默认“中”|
|progress|INTEGER|进度百分比 0-100，默认0|
|owner_id|INTEGER|外键关联 owners 表|
|deadline|TEXT|截止日期时间，ISO格式(YYYY-MM-DD HH:MM)|
|remind_before|INTEGER|提前提醒分钟数，默认30|
|created_at|TEXT|创建时间|
|updated_at|TEXT|最后更新时间|

### 5.2 责任人表 (owners)

|字段名|类型|说明|
|---|---|---|
|id|INTEGER|主键，自增|
|name|TEXT|姓名，唯一|
|email|TEXT|邮箱（可选）|
|color|TEXT|标签颜色|

### 5.3 附件表 (attachments)

|字段名|类型|说明|
|---|---|---|
|id|INTEGER|主键，自增|
|task_id|INTEGER|关联任务ID，级联删除|
|file_name|TEXT|原始文件名|
|saved_path|TEXT|在附件目录中的相对路径|
|added_at|TEXT|添加时间|

### 5.4 链接表 (links)

|字段名|类型|说明|
|---|---|---|
|id|INTEGER|主键，自增|
|task_id|INTEGER|关联任务ID|
|url|TEXT|链接地址|
|title|TEXT|链接显示名称|
|added_at|TEXT|添加时间|

## 6 界面设计（CustomTkinter 组件）

所有窗口使用 `CTk` 作为根窗口，支持深色/浅色主题切换。

### 6.1 主窗口布局

- **左侧筛选面板**（`CTkFrame`，宽度约200px）：
    
    - 搜索输入框（`CTkEntry`）+ 搜索按钮
        
    - 状态过滤下拉框（`CTkOptionMenu`：全部/未开始/进行中/已完成）
        
    - 责任人过滤下拉框（动态加载 Owner 列表）
        
    - 优先级过滤下拉框
        
    - 截止日期范围（两个 `CTkEntry` + 日历选择按钮，可使用 `tkcalendar` 或简单输入）
        
    - “清除筛选”按钮
        
    - “管理责任人”按钮
        
- **右上方工具栏**（`CTkFrame`）：
    
    - “新建任务”按钮（主操作，醒目的颜色）
        
    - 主题切换开关
        
- **任务列表区域**（`CTkScrollableFrame`）：
    
    - 每个任务以卡片形式展示（`CTkFrame`），包含：
        
        - 标题（粗体）、状态图标/标签、优先级颜色条
            
        - 负责人姓名、截止时间（逾期变红）
            
        - 进度条（`CTkProgressBar`，可选）
            
        - 操作按钮：编辑（铅笔图标）、删除（垃圾桶）、详情/展开
            
    - 点击“详情”可展开或弹出详情对话框，展示描述、附件列表、链接列表、进度更新入口。
        
- **状态栏**（底部）：显示当前筛选结果数量、当前时间、下次提醒时间。
    

### 6.2 任务编辑/新建对话框

- 模态 `CTkToplevel` 窗口
    
- 标题输入框、描述文本框（`CTkTextbox`，多行）、优先级选择（`CTkOptionMenu`）
    
- 状态选择、进度滑动条（`CTkSlider`）或数字输入
    
- 负责人下拉选择
    
- 截止日期和时间选择（使用 `CTkEntry` 配合验证，可加简易日历）
    
- 提前提醒时间（分钟数输入）
    
- **附件管理区域**：
    
    - “添加附件”按钮（打开文件选择对话框 `filedialog`）
        
    - 附件列表（`CTkScrollableFrame`），每项显示文件名，“打开”“删除”按钮
        
- **链接管理区域**：
    
    - URL 输入框 + 标题输入框 + “添加链接”按钮
        
    - 链接列表，每项显示标题（可点击打开浏览器），“删除”按钮
        
- 保存 / 取消按钮
    

### 6.3 责任人管理窗口

- 列表显示所有 Owner（`CTkScrollableFrame`），每项：姓名、邮箱、颜色标签
    
- “添加责任人”按钮，弹出简单输入框（姓名、邮箱、颜色选择）
    
- 编辑、删除操作
    
- 删除时检查是否有任务关联，给出警告
    

### 6.4 提醒弹窗

- 使用 `CTkToplevel` 制作非阻塞提醒卡片，或使用 `plyer` 系统通知。
    
- 弹窗内容：任务标题、截止时间、剩余时间，“查看任务”按钮，“稍后提醒”（延迟10分钟），“关闭”
    

## 7 核心模块设计

### 7.1 数据访问层（`database.py`）

类 `Database`：

- 初始化连接，创建表（`CREATE TABLE IF NOT EXISTS`）
    
- 提供通用方法：`execute_query`, `execute_update`, `fetch_all`, `fetch_one`
    
- 事务支持，自动提交与回滚
    
- 关闭连接
    

### 7.2 任务管理业务逻辑（`task_manager.py`）

类 `TaskManager`：

- `create_task(data)`: 插入 tasks 表，返回 id
    
- `update_task(task_id, data)`: 更新
    
- `delete_task(task_id)`: 删除任务及关联附件/链接
    
- `get_task(task_id)`: 返回单任务详情
    
- `search_tasks(filters)`: 支持多条件组合查询，返回列表，JOIN owners
    
- `update_progress(task_id, progress)`: 更新进度，自动转换状态（进度100%→已完成等）
    

### 7.3 责任人管理（`owner_manager.py`）

类 `OwnerManager`：

- CRUD 操作
    
- `get_all_owners()` 用于下拉列表填充
    
- `get_owner_name(id)` 获取姓名
    

### 7.4 附件处理（`attachment_handler.py`）

类 `AttachmentHandler`：

- 附件存储目录：`data/attachments/`，程序所在目录下
    
- `add_attachment(task_id, file_path)`：
    
    1. 生成唯一文件名（UUID或时间戳+原扩展名）
        
    2. 复制到附件目录
        
    3. 在附件表中插入记录
        
- `delete_attachment(attach_id)`：删除文件和记录
    
- `open_attachment(attach_id)`：使用 `os.startfile`（Windows）或 `subprocess.call(('open', file_path))`（macOS）或 `xdg-open`（Linux）打开
    
- `get_attachments(task_id)`：返回列表
    

### 7.5 链接管理（`link_manager.py`）

类 `LinkManager`：

- `add_link(task_id, url, title)`：插入链接表
    
- `delete_link(link_id)`
    
- `get_links(task_id)`：返回链接列表
    
- `open_link(url)`：调用 `webbrowser.open(url)`
    

### 7.6 提醒服务（`reminder_service.py`）

类 `ReminderService`：

- 在独立线程中运行，每30秒检查一次数据库。
    
- 查询条件：`status != '已完成' AND deadline IS NOT NULL AND deadline <= datetime('now', '+N minutes')`，其中 N 为提前提醒分钟数。
    
- 对于需要提醒的任务，通过队列将提醒信息传递给 GUI 主线程（使用 `tkinter` 的 `after` 调用），避免跨线程操作 GUI。
    
- 维护已提醒集合，避免重复提醒（以 task_id 为键，记录下次可提醒时间，如果用户选择“稍后提醒”）。
    
- 可选使用 `plyer.notification` 发送系统通知，点击通知可激活窗口。
    

## 8 提醒机制详细设计

1. **定时检查**：使用 `threading.Thread` 运行检查循环，`time.sleep(60)`，低资源消耗。
    
2. **提醒触发条件**：
    
    - 当前时间 >= `deadline - remind_before 分钟`
        
    - 任务未完成
        
    - 今天尚未提醒过该任务（可每日重置）
        
3. **提醒方式**：
    
    - 若 `plyer` 可用，发送桌面通知。
        
    - 同时在主界面生成一个 `CTkToplevel` 提醒列表窗口，显示所有需提醒的任务。
        
    - 用户可选择“查看任务”打开编辑窗口，或“延时10分钟”。
        
4. **延时逻辑**：将 remind_before 临时增加10分钟，或记录延时时间戳到内存中，下次检查时重新评估。
    

## 9 附件和链接管理细节

- **附件存储策略**：不直接引用原路径，而是复制到 `./data/attachments/` 下，保证任务可迁移和备份。文件重命名为 `{task_id}_{uuid}{ext}` 避免冲突。
    
- **文件大小限制**：可在添加时限制（如 50MB），避免过大。
    
- **链接验证**：添加时可简单检查 URL 格式（正则），不强制联网验证。
    
- **备份**：数据库和附件目录可整体打包备份。
    

## 10 开发计划与迭代建议

1. **第一阶段：基础框架**
    
    - 搭建主窗口布局（筛选栏、列表、工具栏）
        
    - 数据库建表与基础 CRUD
        
    - 任务列表展示（卡片式）与新建/编辑对话框
        
2. **第二阶段：核心功能完善**
    
    - Owner 管理窗口与关联
        
    - 进度管理，状态自动转换
        
    - 附件和链接的增删查
        
3. **第三阶段：提醒与优化**
    
    - 提醒服务集成
        
    - 搜索与筛选功能完善
        
    - UI/UX 优化（颜色、图标、响应式）
        
4. **第四阶段：测试与打包**
    
    - 异常处理与日志
        
    - 使用 PyInstaller 打包成可执行文件
        
    - 编写使用文档
        

## 11 注意事项与可行性分析

- **跨平台兼容**：`plyer` 和文件打开命令需区分操作系统。
    
- **线程安全**：提醒服务只读取数据库，不直接修改 GUI，通过 `queue.Queue` 与主线程通信。
    
- **性能**：SQLite 在百量级任务时性能足够，无需考虑并发。
    
- **用户友好**：CustomTkinter 提供现代组件，卡片式布局直观，支持明暗主题。
    
- **扩展性**：后续可升级为客户端/服务器架构（如换用 MySQL），或添加多用户登录，但当前方案能满足单机小团队使用。
    

## 12 总结

本设计方案基于 Python CustomTkinter 构建本地桌面任务管理系统，覆盖了 CRUD、责任人分配、进度管理、定时提醒、附件与链接管理等全部要求。采用 SQLite 轻量存储，模块化设计清晰，开发风险低，完全可行。该方案可帮助团队或个人高效组织任务，提升生产力。